MCP Agents folder

Place agent implementations here. Agents may modify only the following folders at runtime:
- features/
- steps/
- tests/
- ui/
- api/
- db/

Agents are disabled in CI. See `ai/orchestration/agentOrchestrator.ts` for runtime orchestration.
