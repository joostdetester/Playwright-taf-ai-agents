# Required for UI tests
BASE_URL=
API_BASE_URL=

# UI test credentials (do not commit real values)
# Preferred variable names:
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
# Backward-compatible alternatives (accepted by some steps):
UI_USER_EMAIL=
UI_USER_PASSWORD=

# MCP
MCP_ENABLED=false

# Database (optional for now)
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=